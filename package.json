{
  "name": "secuprobe-scan-action",
  "version": "1.0.0",
  "description": "SecuProbe GitHub Action — security scan in CI/CD",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "npx jest"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}
